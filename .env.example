# Add the real value in Netlify: Project configuration → Environment variables.
# Keep it server-only; never prefix this variable with NEXT_PUBLIC_.
DATABASE_URL=postgresql://xata:password@your-branch.eu-central-1.xata.tech/xata?sslmode=require
